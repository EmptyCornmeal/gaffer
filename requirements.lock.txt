# Gaffer — resolved dependency set. THE install target for every environment.
#
#     pip install -r requirements.lock.txt
#     pip install -e . --no-deps        # editable: see README "Paths"
#
# Covers production + [ai] + [dev] + [mcp] in one file. There is deliberately no
# second requirements file: two of them disagree eventually, and the one CI reads
# is never the one you edited.
#
# Resolved on CPython 3.14 (Windows) from `pip install ".[ai,dev,mcp]"` in an
# empty venv. `pyproject.toml` holds the *ranges*; this file holds what those
# ranges actually resolved to. Regenerate with
# `python -m gaffer.deps --regenerate-hint`, verify with `python -m gaffer.deps`.
#
# Deliberate caps live in pyproject.toml, not here — notably pulp<4.0, which
# removes two APIs the solver calls on every run.
PuLP==3.3.2
PyJWT==2.13.0
PyYAML==6.0.3
Pygments==2.20.0
annotated-types==0.8.0
anthropic==0.120.2
anyio==4.14.2
attrs==26.1.0
certifi==2026.7.22
cffi==2.1.1
click==8.4.2
cryptography==50.0.0
distro==1.9.0
docstring_parser==0.18.0
h11==0.16.0
httpcore2==2.9.1
httpcore==1.0.9
httpx2==2.9.1
httpx==0.28.1
idna==3.18
iniconfig==2.3.0
jiter==0.16.0
jsonschema-specifications==2025.9.1
jsonschema==4.26.0
mcp-types==2.0.0
mcp==2.0.0
numpy==2.5.1
opentelemetry-api==1.44.0
packaging==26.3
pandas==3.0.5
pluggy==1.6.0
pycparser==3.0
pydantic==2.13.4
pydantic_core==2.46.4
pytest==9.1.1
python-dateutil==2.9.0.post0
python-multipart==0.0.32
pywin32==312
referencing==0.37.0
rpds-py==2026.6.3
ruff==0.16.1
six==1.17.0
sniffio==1.3.1
sse-starlette==3.4.8
starlette==1.4.1
truststore==0.10.4
typing-inspection==0.4.2
typing_extensions==4.16.0
tzdata==2026.3
uvicorn==0.52.1
colorama==0.4.6 ; sys_platform == "win32"   # pytest colours; Windows-only by pytest's own marker
