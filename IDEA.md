Gaffer — Fantasy Premier League decision engine. Python pipeline (ingest → project → optimise → export) consuming the official FPL API, with a Svelte phone-first front-end deployed to GitHub Pages. Source of truth: EmptyCornmeal/gaffer on GitHub; data refresh runs on GitHub Actions 3× daily. Canonical checkout on the Mac mini at ~/Projects/gaffer. Primary worker: Claude Code; UI: Kimi; reviewer: Codex. Autonomy: code autonomous, merge/architecture approval, destructive denied. Deployment policy unverified — no auto-release target yet.
